import React from "react";

interface AppProps {}
const App: React.FC<AppProps> = ({}) => {
  return (
    <div className="App">
      <header className="App-header">BLAST!</header>
    </div>
  );
};

export default App;
